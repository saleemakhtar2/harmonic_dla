# Minor revision notes

This revision responds to the second referee report.

- Repaired the broken `rho^4` typesetting in the calibration-cost box.
- Regenerated the calibration-cost figure with the confidence symbol `eta`, matching the manuscript.
- Replaced the stray `tau_0` index in Corollary 8.3 by `n_0`.
- Rephrased Remark 9.12 to state that the sufficient constraints meet at `11/24`, without implying a lower-bound theorem.
- Made the initial block condition explicit: `tau_0 >= max(n_geom, ceil(2^(1/kappa)))`.
- Rewrote the death-circle relaunch remark to avoid the degenerate convention of continuum Brownian motion starting on an absorbing boundary.
- Expanded the Green-function stopping argument to explain why equality holds and how polar exceptional boundary points are handled.
- Added a caption note explaining how the diameter-adaptive theorem shifts the feasible exponent region when `xi > 1/2` is verified.
- Audited and explicitly stated the conjugation convention shared by the exact identity and the Neumann operators.

- Corrected the bibliography rendering of Albert Baernstein II using a literal author name, avoiding BibLaTeX suffix mis-parsing.
